import CommandCenter from '@/components/dashboard/CommandCenter';

export default function CommandCenterPage() {
    return (
        <main className="w-full h-full bg-black min-h-screen">
            <CommandCenter />
        </main>
    );
}
